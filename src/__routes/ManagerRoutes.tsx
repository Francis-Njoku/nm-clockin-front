import React from 'react'

import { Routes as ReactRoutes, Route } from 'react-router-dom'

import ManagerRoute from '../utils/auth/managerRoute'

export default function AppRoutes() {
  return <ReactRoutes></ReactRoutes>
}
